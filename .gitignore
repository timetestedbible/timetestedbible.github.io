# Environment / secrets
.env

# Node
node_modules/
package-lock.json

# AI pipeline temp files
data/bdb-ai-checkpoint.json
data/bdb-ai-run.log
data/bdb-ai-errors.json
data/bdb-fmt-checkpoint.json
data/bdb-fmt-run.log
data/bdb-preview.html

# Large uncompressed data (only .gz committed)
data/bdb-formatted.json

# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
*~
.idea/
.vscode/

# Jekyll (in http folder)
http/_site/
http/.sass-cache/
http/.jekyll-cache/
http/.jekyll-metadata
http/vendor/
http/Gemfile.lock

# TIPNR enrichment script and outputs
enrich_tipnr.py
tipnr-enrichment.log
tipnr-enrichment-progress.json
tipnr-flagged-for-review.json
enrichment.log

# Development notes (local only)
notes/

# Trash - deprecated files kept locally but not tracked
http/.trash/

# Legacy app versions — kept locally for migration reference only
http-v1/
http-v2/

# MorphHB source repo (cloned for build only, not committed)
morphhb-source/

# BibleRank generated data (large files, not committed)
brank/*.dat
brank/*.json
brank/.venv/
